# Этап 4: UI — загрузка файлов и выбор языков

## Цель
Реализовать интерфейс загрузки файлов и выбора направления перевода на странице `document.html`. Включает: drag & drop зону, выбор файлов через диалог, валидацию форматов, селекторы языков, и модальное окно выбора листов для Excel.

## Контекст

### Страница document.html
Создана в Этапе 1. Содержит:
- Шапку с заголовком и переключателем языка (EN/RU)
- Проверку авторизации через background script
- Базовую i18n систему
- Функцию `sendMessage()` для связи с background.js

### Доступные языки
Получаются через `sendMessage({ type: 'GET_LANGUAGES' })`, возвращают:
```javascript
[
  { code: 'AR', name: 'Arabic' },
  { code: 'BG', name: 'Bulgarian' },
  // ... 32+ языка
  { code: 'RU', name: 'Russian' },
  { code: 'EN', name: 'English' },
  // ...
]
```

### Поддерживаемые форматы (из Этапа 3)
- `.xlsx` — Excel (новый формат)
- `.xls` — Excel (старый формат)
- `.docx` — Word
- `.pdf` — PDF (только с текстовым слоем)

### Дизайн-система
Цвета и стили из popup.html:
- Основной: `#25D366` (зелёный)
- Hover: `#128C7E` (тёмно-зелёный)
- Ошибка: `#e53e3e`
- Предупреждение: `#ed8936`
- Фон секций: `#f8f8f8`
- Бордеры: `#ddd`
- Шрифт: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif`

---

## Задачи

### 4.1. Блок выбора языков

Расположение: верхняя часть основного контента страницы, под шапкой.

**Элементы:**
1. **Исходный язык** — `<select>`:
   - Первая опция: "Автоопределение" / "Auto Detect" (value: `auto`)
   - Далее: все языки из `GET_LANGUAGES`
   - По умолчанию: `auto`

2. **Кнопка смены направления** — иконка ⇄:
   - Меняет местами значения source и target
   - Если source = `auto` — не менять, показать подсказку

3. **Целевой язык** — `<select>`:
   - Все языки из `GET_LANGUAGES`
   - По умолчанию: `RU` (Russian)

**Layout:**
```
┌──────────────────────────────────────────────┐
│  [Из: Auto Detect ▾]  ⇄  [В: Russian ▾]    │
└──────────────────────────────────────────────┘
```

Горизонтальное расположение, `display: flex`, `gap: 16px`, `align-items: center`.

### 4.2. Зона загрузки файлов

Расположение: под блоком языков.

**Drag & Drop зона:**
```
┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
│                                               │
│   📄 Перетащите файлы сюда                    │
│      или                                      │
│   [Выбрать файлы]                             │
│                                               │
│   Поддерживаемые форматы: .xlsx, .docx, .pdf  │
│                                               │
└ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
```

**Реализация:**
- Область с пунктирной границей (`border: 2px dashed #ccc`)
- При наведении файла (`dragover`) — подсветка (`border-color: #25D366`, `background: rgba(37, 211, 102, 0.05)`)
- Скрытый `<input type="file" multiple accept=".xlsx,.xls,.docx,.pdf">`
- Кнопка "Выбрать файлы" триггерит `input.click()`
- Поддержка множественного выбора (`multiple`)

**Обработка drop/select:**
1. Получить список файлов из `event.dataTransfer.files` или `input.files`
2. Для каждого файла:
   - Проверить расширение (`.xlsx`, `.xls`, `.docx`, `.pdf`)
   - Если формат не поддерживается — показать ошибку рядом с файлом
   - Проверить размер (максимум 50MB на файл)
3. Добавить файлы в список (не заменять, а дополнять — чтобы можно было загружать файлы несколько раз)
4. Показать список загруженных файлов под зоной

### 4.3. Список загруженных файлов

Расположение: под зоной drag & drop.

**Для каждого файла карточка:**
```
┌──────────────────────────────────────────────┐
│ 📊 report.xlsx          12.3 KB    [✕]       │
│ Листы: Sheet1, Sheet2, Итоги                 │
│ [✓ Sheet1] [✓ Sheet2] [☐ Итоги]              │
└──────────────────────────────────────────────┘
┌──────────────────────────────────────────────┐
│ 📄 contract.docx        45.1 KB    [✕]       │
└──────────────────────────────────────────────┘
┌──────────────────────────────────────────────┐
│ 📕 manual.pdf           1.2 MB     [✕]       │
└──────────────────────────────────────────────┘
```

**Элементы карточки:**
- Иконка формата (📊 Excel, 📄 Word, 📕 PDF — или SVG иконки)
- Имя файла
- Размер файла (humanReadable: KB, MB)
- Кнопка удаления [✕]
- Для Excel: чекбоксы выбора листов (после парсинга структуры)

**Логика для Excel:**
1. При добавлении Excel-файла → вызвать `parseExcel(file)` для получения списка листов
2. Показать чекбоксы с именами листов
3. По умолчанию все листы выбраны
4. Пользователь может снять/поставить чекбоксы
5. Должна быть кнопка "Выбрать все" / "Снять все"

**Для PDF:**
1. При добавлении PDF → проверить наличие текстового слоя через `parsePDF(file)`
2. Если текстового слоя нет — показать предупреждение на карточке:
   ```
   ⚠️ В PDF отсутствует текстовый слой. Файл не может быть переведён.
   ```
3. Подсветить карточку жёлтым/оранжевым
4. Не включать этот файл в перевод

### 4.4. Кнопка запуска перевода

Расположение: под списком файлов.

```
┌──────────────────────────────────────────────┐
│  [🔄 Перевести (3 файла, ~15,200 символов)]  │
└──────────────────────────────────────────────┘
```

**Элементы:**
- Кнопка с классом `btn btn-primary`
- Текст: "Перевести (N файлов, ~X символов)" / "Translate (N files, ~X chars)"
- Счётчик символов обновляется при добавлении/удалении файлов и изменении выбора листов
- Кнопка disabled если:
  - Нет файлов
  - Все файлы с ошибками
  - Перевод уже запущен

**При нажатии:**
- Запускает процесс перевода (Этап 5)
- Кнопка меняет текст на "Переводится..." и становится disabled
- Показывается общий прогресс-бар

### 4.5. Предупреждение о лимитах

Перед запуском перевода:
1. Получить текущий usage через `sendMessage({ type: 'GET_USAGE' })`
2. Сравнить с оценкой символов
3. Если `currentUsage + estimatedChars > limit` — показать предупреждение:
   ```
   ⚠️ Перевод может превысить ваш лимит символов.
   Осталось: 5,000 символов. Требуется: ~15,200 символов.
   [Продолжить всё равно] [Отмена]
   ```
4. Если лимит `isUnlimited` — не показывать предупреждение

---

## i18n ключи для добавления

```javascript
// EN
docSourceLang: 'From language',
docTargetLang: 'To language',
docAutoDetect: 'Auto Detect',
docSwapLangs: 'Swap languages',
docDropZoneTitle: 'Drop files here',
docDropZoneOr: 'or',
docDropZoneBtn: 'Select files',
docDropZoneFormats: 'Supported formats: .xlsx, .docx, .pdf',
docUnsupportedFormat: 'Unsupported format',
docFileTooLarge: 'File too large (max 50MB)',
docNoTextLayer: 'PDF has no text layer. Cannot be translated.',
docSheets: 'Sheets',
docSelectAll: 'Select all',
docDeselectAll: 'Deselect all',
docTranslateBtn: 'Translate ({count} files, ~{chars} chars)',
docTranslating: 'Translating...',
docLimitWarning: 'Translation may exceed your character limit.',
docLimitRemaining: 'Remaining: {remaining} chars. Required: ~{required} chars.',
docContinue: 'Continue anyway',
docCancel: 'Cancel',
docRemoveFile: 'Remove file',

// RU
docSourceLang: 'Язык оригинала',
docTargetLang: 'Язык перевода',
docAutoDetect: 'Автоопределение',
docSwapLangs: 'Поменять местами',
docDropZoneTitle: 'Перетащите файлы сюда',
docDropZoneOr: 'или',
docDropZoneBtn: 'Выбрать файлы',
docDropZoneFormats: 'Поддерживаемые форматы: .xlsx, .docx, .pdf',
docUnsupportedFormat: 'Формат не поддерживается',
docFileTooLarge: 'Файл слишком большой (макс. 50MB)',
docNoTextLayer: 'В PDF отсутствует текстовый слой. Перевод невозможен.',
docSheets: 'Листы',
docSelectAll: 'Выбрать все',
docDeselectAll: 'Снять все',
docTranslateBtn: 'Перевести ({count} файлов, ~{chars} симв.)',
docTranslating: 'Переводится...',
docLimitWarning: 'Перевод может превысить ваш лимит символов.',
docLimitRemaining: 'Осталось: {remaining} симв. Требуется: ~{required} симв.',
docContinue: 'Продолжить',
docCancel: 'Отмена',
docRemoveFile: 'Удалить файл',
```

---

## Файлы для изменения
- `extension/src/document/document.html` — добавить HTML-разметку для всех UI-компонентов
- `extension/src/document/document.js` — добавить логику загрузки, валидации, UI-взаимодействия
- `extension/src/document/document.css` — добавить стили для drop-зоны, карточек файлов, селекторов

## Файлы для создания
- Нет (все файлы созданы в Этапах 1 и 3)

---

## Критерии приёмки

1. Селекторы языков заполняются данными с backend
2. Кнопка ⇄ меняет направление перевода
3. Drag & drop зона принимает файлы с визуальной обратной связью
4. Кнопка "Выбрать файлы" открывает системный диалог с фильтром форматов
5. Неподдерживаемые форматы показывают ошибку
6. Файлы > 50MB отклоняются с сообщением
7. Для Excel отображаются чекбоксы листов
8. Для PDF без текста — предупреждение
9. Счётчик символов обновляется динамически
10. Предупреждение о лимитах работает корректно
11. Все тексты имеют i18n (EN/RU)
12. Интерфейс адаптивный и аккуратный

## Зависимости
- Этап 1 (страница document.html, авторизация, i18n)
- Этап 3 (парсеры для предварительного анализа файлов — список листов Excel, проверка PDF)
